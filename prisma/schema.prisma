generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Projet {
  title          String @id
  slug           String
  description_fr String
  description_en String
  link           String
  nbr_images     Int    @default(0)
}
